-- ============================================================================
-- V14__add_play_count_to_map.sql
-- map 테이블에 누적 플레이 횟수 컬럼 추가
-- ============================================================================

ALTER TABLE map
    ADD COLUMN play_count BIGINT NOT NULL DEFAULT 0;